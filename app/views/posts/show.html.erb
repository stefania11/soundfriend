<p>
  <strong>Song Name:</strong>
  <%= @song.html_safe %>
</p>

<p>
  <strong>Song:</strong>
  <%= @post.track_url %>
</p>

<p>
  <strong>Add to playlist:</strong>
  <%= form_tag post_path do  %>
    <%= select_tag(:playlist) do %>
        <%= content_tag(:option, @playlists.first["title"], value: @playlists.first["id"]) %>
    <% end %>
  <% end %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
